import Cat from "../features/ui/cat";
import { StyledContainer } from "../shared/ui/container/styled";

const Home = () => {
  return (
    <StyledContainer>
      <Cat />
    </StyledContainer>
  );
};

export default Home;
